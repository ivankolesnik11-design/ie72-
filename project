import tkinter as tk
from PIL import Image, ImageTk
import random
import time

class ThermalApp:
    def __init__(self, root):
        self.root = root
        self.root.title("Система Термографии")
        self.root.geometry("800x600")
        self.main_frame = tk.Frame(root)
        self.main_frame.pack(fill=tk.BOTH, expand=True)
        self.show_initial_selection()

    def show_initial_selection(self):
        for widget in self.main_frame.winfo_children():
            widget.destroy()
            
        label = tk.Label(self.main_frame, text="Выберите объект наблюдения:", font=("Arial", 16))
        label.pack(pady=20)
        
        btn_frame = tk.Frame(self.main_frame)
        btn_frame.pack(pady=10)
        
        # Кнопки пока без действий
        btn_human = tk.Button(btn_frame, text="Человек", width=15, height=2)
        btn_human.grid(row=0, column=0, padx=10)
        
        btn_animal = tk.Button(btn_frame, text="Животное", width=15, height=2)
        btn_animal.grid(row=0, column=1, padx=10)

if __name__ == "__main__":
    root = tk.Tk()
    app = ThermalApp(root)
    root.mainloop()
